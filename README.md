# wakefield-cultural-assets
A wrapper website for a cultural asset survey in Wakefield using Carto, LocalData, and NodeJS

# Development
`bundle install`
`middleman serve`

# Deploy
Simple push to `master` and Travis CLI is setup to build through Webhooks.
